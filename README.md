# Document Management Dashboard (Prototype)

Lightweight full-stack prototype for uploading and processing PDF documents.

This workspace contains the initial project skeleton and styling configuration.

Commits included:
- Commit 1 — init: project skeleton and README
- Commit 2 — chore: add Tailwind + Livvic font + theme tokens

Run locally:

```bash
npm install
npm run dev
```

Next steps: implement upload API, per-file upload UI, and real-time notifications.
